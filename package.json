{
  "name": "Radar-Jrmy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"require('fs').copyFileSync('data/data.json','src/data.json')\" && python -m http.server 8080 --directory src"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "htmlhint": "^1.1.4"
  }
}
